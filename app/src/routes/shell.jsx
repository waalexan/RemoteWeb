import React from 'react';
import Terminal from '../../components/web-terminal';

export const Shell = () => {
    return (
        <div className="h-screen">
            <Terminal />
        </div>
    )
};